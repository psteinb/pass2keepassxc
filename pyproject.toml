[project]
name = "pass2keepassxc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-gnupg>=0.5.5",
    "tqdm>=4.66",
]

[project.scripts]
pass2keepassxc = "main:main"
